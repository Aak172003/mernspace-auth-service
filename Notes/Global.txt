How install as a dev dependencies

npm install -D <package-name>
or 
npm install --save-dev <package-name>

------------------------------------------------------------------------------------------------------------------------

We can pass env variable like this 
PORT=5555 node dist/server.js  (But this will only work on git bash terminal)

------------------------------------------------------------------------------------------------------------------------

// This is one way to read all variables from .env files 
import { config } from "dotenv";
config();

// This is second way to read all variables from .env files 
import 'dotenv/config'

------------------------------------------------------------------------------------------------------------------------

In express by default every json travel in plain text format 

------------------------------------------------------------------------------------------------------------------------


We can make dotenv config read environment varible as dynamically nased on our NODE_ENV it will read environment variable based on NODE_ENV 
This module path is work dynamically based on operating systems 

config({
    path: path.join(__dirname, `../../.env.${process.env.NODE_ENV ?? "dev"}`),
});


If i assess any function which present inside class with creating an instance in controller 
This make our application highly coupled -> which means without UserService our any controller never work 
To avoid this highly coupled approach we hav eto do dependecy injection

const userService = new UserService();
await userService.create({ firstName, lastName, email, password });

How we can do just create contructor and revice that instace in its contructor